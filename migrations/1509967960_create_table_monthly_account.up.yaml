- args:
    sql: CREATE TABLE "monthly_account"(id serial NOT NULL, src integer NOT NULL,
      account_id text NOT NULL, status integer NOT NULL, created_at timestamptz NOT
      NULL, PRIMARY KEY (id) )
  type: run_sql
- args:
    name: monthly_account
  type: add_existing_table_or_view
